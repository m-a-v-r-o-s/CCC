import BuildsPageContent from "@/components/BuildsPageContent";

export const metadata = { title: "Builds — Cycles Custom Cult" };

export default function BuildsPage() {
  return <BuildsPageContent />;
}
